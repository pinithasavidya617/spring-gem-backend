spring:
  application:
    name: gembackend

  datasource:
    url: jdbc:h2:mem:gemdb
    username: admin
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8081